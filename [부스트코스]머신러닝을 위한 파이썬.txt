list.split()

' '.join(list)

[i+j for i in range(10) for j in range(10) if j%2==0]

enumerate(list)

zip(tuple)

pandas에서 사용
--------------------------
f = lambda x, y: x + y

map

from functools import reduce
reduce
--------------------
asterisk                      ##몇개의 인자를 받게 될지 모를때
def aesterisk_test(a,*args)  #첫 인자만 들어가고 나머지는 튜플로 저장
def aesterisk_test(a,**kargs) #첫 인자만 들어가고 나머지는 딕트로 저장
튜플 앞에 *을 붙이면 언패킹된다
딕트 앞에 **을 붙이면 언패킹된다
==========================================================chp.1